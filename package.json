{
  "name": "@piqy/epos-utils",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "benchmark": "vp run -r build && node --expose-gc tools/benchmark.mjs && node tools/benchmark-memory.mjs",
    "build": "vp run -r build",
    "check": "vp check",
    "check:tree-shaking": "vp run -r build && node tools/check-codepage-tree-shaking.mjs",
    "changeset": "changeset",
    "fmt": "vp fmt --write",
    "fmt:check": "vp fmt --check",
    "lint": "vp lint",
    "lint:fix": "vp lint --fix",
    "prepare": "effect-tsgo patch --typescript --oxlint",
    "release": "vp run -r build && changeset publish",
    "test": "vp test",
    "version-packages": "changeset version"
  },
  "devDependencies": {
    "@arethetypeswrong/core": "0.18.5",
    "@changesets/cli": "3.0.1",
    "@effect/tsgo": "0.38.0",
    "@effect/vitest": "4.0.0-rc.110",
    "@oxlint/plugins": "1.80.0",
    "@types/node": "^26.1.1",
    "effect": "catalog:",
    "publint": "^0.3.24",
    "typescript": "^7.0.2",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.24.0"
}
