{
  "name": "@piqy/epos-utils",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm --recursive build",
    "check": "pnpm build && pnpm typecheck",
    "changeset": "changeset",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "release": "pnpm build && changeset publish",
    "typecheck": "pnpm --recursive check",
    "version-packages": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "tsdown": "catalog:",
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.22.0"
}
