---
'@piqy/epos-ast': minor
---

Add an optional Effect Schema entry point for recursive AST validation. Use `type` as the tagged-union discriminant and keep the main AST entry free from Effect runtime imports.
